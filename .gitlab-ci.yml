stages:
  - validate
  - plan
  - deploy

default:
  tags:
    - docker
  image: 
    name: hashicorp/terraform:1.5
    entrypoint: ["/bin/sh", "-c"]

terraform-validate:
  stage: validate
  before_script:
    - terraform --version
    - terraform init
  script:
    - terraform validate

terraform-plan:
  stage: plan
  before_script:
    - terraform init
  script:
    - terraform plan